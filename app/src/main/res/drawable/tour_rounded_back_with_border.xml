<shape xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Background color -->
    <solid android:color="#FFFFFF" />

    <!-- Rounded corners -->
    <corners android:radius="14dp" />

    <!-- Border (Stroke) -->
    <stroke
        android:width="2dp"
        android:color="#0481BA" />

</shape>
